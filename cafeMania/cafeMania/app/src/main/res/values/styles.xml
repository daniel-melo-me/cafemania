<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/dark_brown</item>
        <item name="colorPrimaryDark">@color/light_brown</item>
        <item name="colorAccent">@color/beige</item>
        <item name="android:windowBackground">@color/cream</item>
    </style>

    <!-- Estilo botão deletar -->
    <style name="DeleteButtonStyle" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textColor">#FFFFFF</item> <!-- Cor do texto -->
        <item name="android:background">@drawable/rounded_button</item> <!-- Background arredondado -->
        <item name="android:textSize">14sp</item> <!-- Tamanho do texto -->
        <item name="android:padding">8dp</item> <!-- Padding do botão -->
    </style>

    <!-- Estilo menus -->
    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:textSize">18sp</item>
        <item name="android:padding">12dp</item>
        <item name="android:textColor">@color/light_brown</item>
        <item name="android:background">@drawable/button_background</item>
    </style>
</resources>
